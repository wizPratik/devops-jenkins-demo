const maxNum = require("./maxNum.js");
const addNum = require("./addNum.js");

console.log(
	"The maximum number from 10, 50, 100, 1000 is %d",
	maxNum(10, 50, 100, 1000)
);

console.log("Addition of 10 and 20 is %d", addNum(10, 20));
